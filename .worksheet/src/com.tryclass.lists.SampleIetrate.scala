package com.tryclass.lists

object SampleIetrate {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(76); 
 	val pairs = List(1,2,3);System.out.println("""pairs  : <error> = """ + $show(pairs ))}
}
