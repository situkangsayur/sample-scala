package com.tryclass.objects

object SampleIetrate {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(78); 
 	val pairs = List(1,2,3);System.out.println("""pairs  : List[Int] = """ + $show(pairs ))}
}
